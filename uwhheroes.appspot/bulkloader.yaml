# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Country
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code

    - property: name
      external_name: name


- kind: Division
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code

    - property: name
      external_name: name


- kind: GameType
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code

    - property: name
      external_name: name


- kind: Person
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: firstName
      external_name: firstName

    - property: lastName
      external_name: lastName


#Dont use Player anymore
- kind: Player
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: firstName
      external_name: firstName

    - property: girlyBits
      external_name: girlyBits
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: lastName
      external_name: lastName


- kind: Position
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code

    - property: name
      external_name: name


- kind: Rank
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: code
      external_name: code

    - property: name
      external_name: name


- kind: Team_Official
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: person
      external_name: person
      import_transform: transform.create_foreign_key('Person')
      export_transform: transform.key_id_or_name_as_string

    - property: team
      external_name: team
      import_transform: transform.create_foreign_key('Team')
      export_transform: transform.key_id_or_name_as_string

    - property: title
      external_name: title


- kind: Team_Player
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: person
      external_name: person
      import_transform: transform.create_foreign_key('Person')
      export_transform: transform.key_id_or_name_as_string

    - property: position
      external_name: position
      import_transform: transform.create_foreign_key('Position')
      export_transform: transform.key_id_or_name_as_string

    - property: rank
      external_name: rank
      # Type: Key Stats: 112 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Rank')
      export_transform: transform.key_id_or_name_as_string

    - property: team
      external_name: team
      # Type: Key Stats: 112 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Team')
      export_transform: transform.key_id_or_name_as_string


- kind: Team
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: coach
      external_name: coach
      import_transform: transform.create_foreign_key('Person')
      export_transform: transform.key_id_or_name_as_string

    - property: country
      external_name: country
      import_transform: transform.create_foreign_key('Country')
      export_transform: transform.key_id_or_name_as_string

    - property: division
      external_name: division
      import_transform: transform.create_foreign_key('Division')
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name

    - property: tournament
      external_name: tournament
      import_transform: transform.create_foreign_key('Tournament')
      export_transform: transform.key_id_or_name_as_string


- kind: Tournament
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: city
      external_name: city

    - property: country
      external_name: country
      import_transform: transform.create_foreign_key('Country')
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name

    - property: startDate
      external_name: startDate
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

